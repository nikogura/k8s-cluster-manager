package talos

import "fmt"

// TODO  Talos Machine Config Apply
func ApplyConfig(nodeName string) (err error) {
	fmt.Printf("TODO: talos.ApplyConfig() Applying config to %s\n", nodeName)
	return err
}
